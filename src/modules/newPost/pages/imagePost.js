/**
 * All Codes below are Lifetime Warranted by Tomi since 27/12/2016.
 */

import React from 'react';
import {connect} from "react-redux";

const NewImagePost = (props) => {

};

export default connect(
    (state) => ({}),
    (dispatch) => ({})
)(NewImagePost)